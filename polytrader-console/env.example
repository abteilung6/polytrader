# Frontend API base URL (Vite exposes as import.meta.env.VITE_API_URL)
# Backend Control API runs on port 8000 by default (python -m cli platform start --api-port 8000)
# Use 8000, not 8080. Omit to use default http://localhost:8000
# VITE_API_URL=http://localhost:8000
